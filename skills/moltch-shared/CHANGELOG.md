# moltch-shared changelog

## v1.2.0 - 2026-03-09
- set autonomy-default direct agent coordination
- added no-relay rule (human relay only when tooling unavailable)
- added single-thread communication default for visible audit trail
- added mode behavior clarification (`discussion_only` vs `implementation_allowed`)
- added handoff SLA windows + required handoff template
- added escalation threshold (`2 unanswered pings` in active window)
- added coordination heartbeat requirement (`done/next/blocked`)
- added weekly KPI `human_relay_touches_per_issue`
